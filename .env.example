# --- ENVIRONMENT CONFIGURATION ---
# Professional agents use .env files to store sensitive API keys.
# Duplicate this file, rename it to '.env', and add your real key.

AI_API_KEY=your_real_api_key_here
DATABASE_URL=sqlite:///./agent_database.db
DEBUG_MODE=True
